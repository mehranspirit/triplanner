export const MAP_VIEW_GLOBAL_KEY = 'tripMapView:global';

export const mapViewTripKey = (tripId: string) => `tripMapView:${tripId}`;

export const mapViewSuggestKey = (tripId: string) => `tripMapView:suggest:${tripId}`;
